<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ===================== -->
    <!-- Padding de tela       -->
    <!-- ===================== -->
    <dimen name="padding_screen_horizontal">18dp</dimen>
    <dimen name="padding_screen_top">18dp</dimen>
    <dimen name="padding_screen_bottom">18dp</dimen>

    <!-- ===================== -->
    <!-- Espaçamentos (spacing) -->
    <!-- ===================== -->
    <dimen name="spacing_xsmall">6dp</dimen>
    <dimen name="spacing_small">10dp</dimen>
    <dimen name="spacing_medium">18dp</dimen>
    <dimen name="spacing_large">24dp</dimen>

    <!-- Compat (se você já usa) -->
    <dimen name="padding_small">8dp</dimen>
    <dimen name="padding_medium">16dp</dimen>
    <dimen name="padding_large">24dp</dimen>
    <dimen name="padding_larger">36dp</dimen>
    <dimen name="margin_standard">16dp</dimen>

    <!-- ===================== -->
    <!-- Tipografia            -->
    <!-- ===================== -->
    <dimen name="text_small">12sp</dimen>
    <dimen name="text_medium">16sp</dimen>
    <dimen name="text_large">20sp</dimen>
    <dimen name="text_xlarge">24sp</dimen>

    <!-- Novos nomes usados no layout -->
    <dimen name="text_title">22sp</dimen>
    <dimen name="text_body">14sp</dimen>

    <!-- (legado seu) -->
    <dimen name="text_size_large">22sp</dimen>

    <!-- ===================== -->
    <!-- Componentes           -->
    <!-- ===================== -->
    <dimen name="button_height">56dp</dimen>

    <dimen name="image_size_large">120dp</dimen>
    <dimen name="image_size_medium">80dp</dimen>
    <dimen name="image_size_small">40dp</dimen>

    <dimen name="icon_size">120dp</dimen>

    <!-- ===================== -->
    <!-- Labels / Pills         -->
    <!-- ===================== -->
    <dimen name="padding_label_horizontal">12dp</dimen>
    <dimen name="padding_label_vertical">6dp</dimen>

    <!-- ===================== -->
    <!-- Outros                -->
    <!-- ===================== -->
    <dimen name="option_card_stroke">2dp</dimen>

</resources>
